

let punishingProfessor = prompt("professor lines are for");
let lineToWrite = prompt ("What's the line");
let numOfTines = prompt("How many times must you write it?");
numOfTines = parseInt(numOfTimes);

for (let line = 1; line<numOfTimes; line++) {
    console.log(`${line} : ${lineToWrite}`);
}

